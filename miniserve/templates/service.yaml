apiVersion: v1
kind: Service
metadata:
  name: {{ include "miniserve.fullname" . }}
  labels:
    {{- include "miniserve.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "miniserve.selectorLabels" . | nindent 4 }}
