apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocrserver.fullname" . }}
  labels:
    {{- include "ocrserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocrserver.selectorLabels" . | nindent 4 }}
